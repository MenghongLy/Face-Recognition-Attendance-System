# Face-Recognition-Attendance-System
This project is a Face Recognition Attendance System built using Python. It uses a webcam to detect and recognize student faces, then automatically records attendance into an Excel file with time and status (On Time / Late).
